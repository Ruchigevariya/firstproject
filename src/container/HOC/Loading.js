import React from 'react';

function Loading(Compenent) {
    return Loading ({isloading})
        if(setLoading){
            return(
                <p>Loading....</p>
            )
        }else{
            return{

            }
        }
}

export default Loading;